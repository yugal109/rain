#!/usr/bin/env sh
set -e
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
JAR="$ROOT/dist/rain.jar"

if [ ! -f "$JAR" ]; then
  echo "rain: $JAR not found. Run 'make jar' from the project root." >&2
  exit 1
fi

exec java -jar "$JAR" "$@"
